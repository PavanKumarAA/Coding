#include<stdio.h>
#include<conio.h>
int main()
{
    int count=0;
    char str[100];
    gets(str);
    strupr(str);
    puts(str);
    for(int i=0;str[i]!='\0';i++)
    {
        if((str[i]<'A')||(str[i]>'Z'))
            count++;
    }
    printf("%d",count);
    return 0;
}
