#include <stdio.h>
void main()
{
  int size,i,j;
  char a[20];
  printf("Enter a word \n");
  scanf("%s",a);
  for(size=0;a[size];size++);
  char b[size];
  b[size]='\0';
  for(i=0,j=size-1;i<size;i++,j--)
  {
    b[j]=a[i];
  }
  printf("The reverse form is : %s",b);
}
